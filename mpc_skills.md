
# GSD Phase 控制器（multi-phase-controller，简称 `mpc`）

## 目标

本文档的唯一目的，是为以下 5 个 skills 提供可执行、可并发协作、可持续维护的控制规则：

- `mpc-master-start`
- `mpc-master-progress`
- `mpc-master-regress`
- `mpc-master-archive`
- `mpc-slave`

本文档不是功能宣传文档，而是 5 个 skills 的行为规范。后续实现时，应直接按本规范读写 `~/.mpc/{工程项目名}/mpc.md` 与 `~/.mpc/{工程项目名}/mpc_archive.md`。

## 文件规划

- 共享控制目录：`~/.mpc/{工程项目名}/`
- 实时控制文件：`~/.mpc/{工程项目名}/mpc.md`
- 归档文件：`~/.mpc/{工程项目名}/mpc_archive.md`
- 写入锁文件：`~/.mpc/{工程项目名}/.lock.md`
- 共享控制目录为项目级唯一目录，不跟随具体 worktree 复制
- `工程项目名` 固定取 Git 仓库名字，而不是当前 worktree 目录名
- 在 worktree 环境中，应优先通过 Git 共享仓库信息解析仓库名；禁止直接拿当前 worktree 目录名当作 `工程项目名`
- 命名固定为 `mpc.md`、`mpc_archive.md` 与 `.lock.md`
- `.lock.md` 为临时锁文件，仅用于串行化写入；只读查询类 skill 不创建该文件
- 共享目录或文件不存在时，由 `mpc-master-start` 或首次归档动作创建基础骨架

## 总体原则

- 主控制器负责拆分、汇总、回归、归档，不负责代替子任务做日常开发推进
- 主控制器侧共包含 4 个 skills：`mpc-master-start`、`mpc-master-progress`、`mpc-master-regress`、`mpc-master-archive`
- 其中进度相关职责固定拆分为 3 个独立 skills：`mpc-master-progress`、`mpc-master-regress`、`mpc-master-archive`，三者不得混用职责
- 每个子任务必须独占一个 worktree
- `子任务名称`、`worktree 名称`、`worktree 目录名`、`worktree 分支名` 必须完全一致
- 同一 Git 仓库下的所有 worktree，必须共享同一个 `~/.mpc/{工程项目名}/` 控制目录；不要在当前 worktree 下寻找 `.mpc/`
- 子任务默认要求可独立并行；如果存在依赖，必须显式声明 `前序子任务` 与 `后序子任务`
- 如果两个子任务存在明显代码冲突、强顺序依赖、共享同一核心改动点，则允许拆分，但只能拆成串行子任务，不能拆成并行子任务，且必须明确 `前序子任务` 与 `后序子任务`
- `mpc.md` 必须使用固定字段顺序，字段不可省略；没有值时必须写 `待填写` 或 `无`
- 任何查询动作默认只读；任何状态推进动作必须显式触发，不能把“查看”设计成“顺手修改”
- 所有时间统一使用 `YYYY-MM-DD HH:mm:ss +08:00` 格式
- 除 `mpc-master-progress` 外，所有会写入 `~/.mpc/{工程项目名}/mpc.md` 或 `~/.mpc/{工程项目名}/mpc_archive.md` 的动作都必须先持有 `~/.mpc/{工程项目名}/.lock.md`
- `进行中 -> 完成` 与 `完成 -> 已回归` 的推荐判断标准来自子任务业务本身；最终是否推进状态，由当前调用者决定

## 子任务状态机

子任务固定只有 4 种状态：

- `未开始`
- `进行中`
- `完成`
- `已回归`

唯一允许的正常流转路径如下：

`未开始 -> 进行中 -> 完成 -> 已回归`

状态定义如下：

- `未开始`：任务已被拆分并写入控制器，但对应 worktree 还未正式执行
- `进行中`：子任务已开始执行，允许持续更新进度、阻塞项、预计剩余时间
- `完成`：子任务自身开发与自检已结束，等待主控制器执行回归动作
- `已回归`：主控制器已完成该子任务的回归确认，允许归档

状态推进责任如下：

- `mpc-master-start`：只能创建 `未开始`
- `mpc-slave`：负责 `未开始 -> 进行中` 与 `进行中 -> 完成`；其中 `进行中 -> 完成` 必须基于任务业务目标、自检结果与当前调用者判断
- `mpc-master-progress`：只负责查看，不负责推进状态
- `mpc-master-regress`：负责 `完成 -> 已回归`；其中回归是否通过由当前调用者结合回归证据判断
- `mpc-master-archive`：负责归档 `已回归` 子任务
- 归档不是状态，归档动作只允许发生在 `已回归` 之后

## `mpc.md` 固定结构

`mpc.md` 必须采用以下固定结构。字段顺序固定，后续 skill 应按标题精确读取和更新。

````md
# MPC 控制文件

## 元信息
- 版本：1
- 最后更新时间：2026-04-25 12:00:00 +08:00
- 任务总数：1

# 子任务 phone_1_requirement_1
## 任务标识
phone_1_requirement_1
## 状态
未开始
## 来源计划文件
docs/example_plan.md
## worktree名称
phone_1_requirement_1
## worktree目录
../phone_1_requirement_1
## worktree分支
phone_1_requirement_1
## 前序子任务
无
## 后序子任务
phone_2_requirement_2
## 子任务目标
```text
完成 phone_1_requirement_1 的上下文装载与边界确认。
输出可直接支撑后续实现的当前边界、现状与主要风险结论。
```
## 开始时间
待填写
## 完成时间
待填写
## 回归时间
待填写
## 归档时间
待填写
## 最后更新时间
2026-04-25 12:00:00 +08:00
## 累计耗时
待填写
## 预计剩余时间
待填写
## 当前进度概况
```text
待填写
```
## 阻塞事项
```text
无
```
## 下一步提示词
```text
待填写
```
## 开始提示词
```text
执行 gsdphase 0。
目标：完成子任务目标要求的上下文装载与边界确认。
请输出：
1. 当前边界
2. 现状
3. 对子任务目标的完成判断依据
```
## 完成提示词
```text
请对 phone_1_requirement_1 的产出是否满足“子任务目标”做回归确认，并给出是否允许归档的结论。
```
## 完成摘要
```text
待填写
```
## 回归摘要
```text
待填写
```
## 归档状态
未归档
````

补充约束如下：

- `当前进度概况` 最多 5 行
- `阻塞事项` 最多 5 行；没有阻塞时写 `无`
- `子任务目标` 使用带 `text` 信息字符串的 fenced code block，限制 1 到 5 行，内容必须面向结果、可直接用于完成判定，不能只写过程动作
- `下一步提示词` 用于下一轮继续推进当前子任务
- `累计耗时` 与 `预计剩余时间` 统一使用 `Xd Xh Xm` 文本格式；不足位可省略，例如 `4h 30m`
- `当前进度概况`、`阻塞事项`、`下一步提示词` 没有实质变化时，允许保持原文不变，仅更新 `最后更新时间`
- `完成摘要` 至少包含：完成内容、未完成风险、自检结果、建议主控回归关注点
- `回归摘要` 至少包含：回归范围、依据证据、回归结果、是否允许归档
- `归档状态` 只能是 `未归档` 或 `已归档`

## `mpc_archive.md` 结构

- `mpc_archive.md` 的单个子任务结构与 `mpc.md` 完全一致
- `任务总数` 表示当前文件内任务块数量；在 `mpc.md` 中表示当前活动任务数，在 `mpc_archive.md` 中表示当前归档任务数
- 归档时采用“移动”而不是“复制”
- 被归档的子任务必须从 `~/.mpc/{工程项目名}/mpc.md` 删除，并追加到 `~/.mpc/{工程项目名}/mpc_archive.md`
- 子任务查询与依赖解析的范围，是 `~/.mpc/{工程项目名}/mpc.md` 与 `~/.mpc/{工程项目名}/mpc_archive.md` 的并集；默认优先查询 `~/.mpc/{工程项目名}/mpc.md`，未命中时再查询 `~/.mpc/{工程项目名}/mpc_archive.md`
- 如果同名 `任务标识` 同时出现在 `~/.mpc/{工程项目名}/mpc.md` 与 `~/.mpc/{工程项目名}/mpc_archive.md`，视为数据异常，必须立即停止并报错
- 归档后必须满足：
  - `状态` 为 `已回归`
  - `归档状态` 为 `已归档`
  - `归档时间` 已填写

## 并发写入规则

由于 `mpc.md` 会被多个 worktree 读写，而 skills 又由使用者手动触发，因此真实高并发概率通常较低；但为了满足“不丢状态”的目标，所有写入动作仍必须串行化。skills 必须遵守以下规则：

1. 除 `mpc-master-progress` 外，任何写入动作开始前都必须先申请 `~/.mpc/{工程项目名}/.lock.md`
2. `.lock.md` 内至少记录：持有者 skill 名称、目标任务名称或任务列表、当前 worktree/分支、加锁时间
3. 如果发现 `~/.mpc/{工程项目名}/.lock.md` 已存在，必须等待并重试；默认每 2 秒重试一次，最多等待 60 秒
4. 如果 `~/.mpc/{工程项目名}/.lock.md` 持续超过 10 分钟未释放，应判定为陈旧锁；此时必须停止写入，并明确提示调用者人工处理旧锁
5. 成功获取锁后，必须重新读取最新的 `~/.mpc/{工程项目名}/mpc.md`；必要时同时读取 `~/.mpc/{工程项目名}/mpc_archive.md`
6. 除 `mpc-master-start` 外，每次只允许修改一个子任务块，以及文件头部 `元信息`
7. `mpc-master-start` 允许在单次事务内追加多个新任务块，并以最小改动范围回填直接相关前序任务的 `后序子任务` 字段；不得改写无关任务内容
8. 非目标子任务内容、顺序、提示词不得被重写或折叠
9. 写入前必须校验 `任务标识` 与当前目标一致；`mpc-master-start` 还必须校验待新增任务名称均合法且不重名
10. 如果获取锁后发现当前准备写入的内容不是基于最新文件快照生成，必须基于最新内容重新合并；合并失败时，应停止写入并明确提示冲突，而不是覆盖
11. 写入必须采用“临时文件 + 原子替换”方式，禁止原地覆盖半写入文件
12. 每次成功写入后，必须同步更新：
   - 文件头部 `最后更新时间`
   - 文件头部 `任务总数`
   - 当前子任务的 `最后更新时间`
13. 无论本轮写入成功或失败，只要持有过锁，都必须在结束前释放 `~/.mpc/{工程项目名}/.lock.md`

## 任务拆分规则

`mpc-master-start` 在拆分子任务时必须满足以下条件：

- 子任务名称全局唯一，不能与已有子任务重名
- 子任务名称不能与 `mpc`、`mpc_archive`、`lock` 等保留名称冲突
- 子任务名称只允许使用小写字母、数字、下划线
- 子任务默认以 3 到 10 个可执行步骤为宜
- 只有在强原子性要求下，才允许超过 10 个步骤
- 如果两个子任务存在明显代码冲突、强顺序依赖、共享同一核心改动点，则应拆成串行子任务，而不是并行子任务，并且必须显式写明 `前序子任务` 与 `后序子任务`
- 如果存在弱依赖，也可以拆分，但必须在字段中写明 `前序子任务` 与 `后序子任务`
- 如果是向现有需求链路追加后续任务，允许最小范围修改直接前序任务的 `后序子任务` 字段，以保持依赖图一致
- 串行子任务允许共属于同一需求链路，但启动顺序必须严格受 `前序子任务` 约束

## 任务树输出规范

`mpc-master-start`、`mpc-master-progress`、`mpc-master-regress`、`mpc-master-archive` 在命令结束时，都必须输出一次“最新任务树”。任务树用于稳定展示当前控制器中的任务依赖关系，不能只用平铺列表替代。

- 任务树数据源是 `~/.mpc/{工程项目名}/mpc.md` 与 `~/.mpc/{工程项目名}/mpc_archive.md` 的并集；如果归档文件不存在，可以忽略
- 如果同名 `任务标识` 同时出现在两个文件中，或树构建过程中发现循环依赖、缺失子任务、同一子任务被多个父任务直接声明为后序任务，必须报数据异常并停止，不能静默猜测
- `前序子任务` 与 `后序子任务` 在存在多个任务时，统一使用 `、` 分隔；没有值时写 `无`
- 根节点判定规则：`前序子任务 = 无`
- 子节点判定规则：父任务 `后序子任务` 中列出的直接后序任务
- 根节点输出顺序：优先保持 `mpc.md` 中任务块原始顺序；仅存在于 `mpc_archive.md` 的根节点追加在后，并保持归档文件中的原始顺序
- 子节点输出顺序：严格按父任务 `后序子任务` 中的出现顺序输出
- 树状图字符统一使用 `├─`、`└─`、`│  ` 与 3 个空格缩进，形成类似：

```text
root_task
├─ child_a
│  └─ grandchild_a1
└─ child_b
```

- 树节点最少输出 `任务标识`
- `mpc-master-progress`、`mpc-master-regress`、`mpc-master-archive` 默认应在节点后追加状态标签，如 `task_name [进行中]`
- 已归档任务在树中统一显示为 `task_name [已归档]`
- `mpc-master-start` 在所有新任务初始都为 `未开始` 时，可以只输出任务名；如果当前控制器中已经存在混合状态任务，也允许统一追加状态标签
- `mpc-master-regress` 与 `mpc-master-archive` 的任务树格式必须复用 `mpc-master-start` 的完整树风格，不允许退化为平铺列表或省略层级

## 5 个 Skills 的详细定义

### 1. `mpc-master-start`

#### 手动调用示例

```text
/mpc-master-start docs/xxxxx.md
```

其中 `docs/xxxxx.md` 是需求计划文件。

#### 输入

- 必填：一个需求计划文件路径

#### 行为

1. 读取需求计划文件并分析可并行执行或可串行执行的子任务
2. 为每个子任务生成固定结构的任务块，初始状态统一为 `未开始`
3. 自动生成：
   - `子任务名称`
   - `前序子任务`
   - `后序子任务`
   - `子任务目标`
   - `开始提示词`
   - `完成提示词`
4. 如果 `~/.mpc/{工程项目名}/mpc.md` 已存在：
   - 必须先检查是否有重名任务
   - 默认只允许追加新任务
   - 如果本次是向现有链路追加后续任务，则允许最小范围修改直接前序任务的 `后序子任务` 字段，但不得改写无关任务内容
5. 如果 `~/.mpc/{工程项目名}/mpc.md` 不存在：
   - 先创建 `~/.mpc/{工程项目名}/`
   - 再创建 `mpc.md` 骨架
6. 命令结束时，输出建议执行的 worktree 创建命令列表，但不自动执行

#### 输出要求

- 先按“任务树输出规范”输出本次写入后的最新任务树
- 列出本次新增子任务总数
- 只列出首轮可执行子任务
- 只列出首轮可执行子任务对应的 worktree 创建命令
- 首轮可执行子任务的判定标准是：`前序子任务 = 无`，或其前序子任务已经在现有控制文件中处于 `已回归`
- 被前序子任务阻塞的新任务必须体现在任务树中，但不需要再额外用平铺列表逐条展示；这些任务仍然必须写入 `~/.mpc/{工程项目名}/mpc.md`
- 如果本次新增任务中没有任何首轮可执行项，必须明确输出“首轮可执行子任务：无”，并简要说明阻塞来源，但不要展开打印全部阻塞链路

#### worktree 命令约束

- 命令仅作为建议输出，不由 skill 自动执行
- 用户可以手动执行，或使用 Codex 的 worktree 功能创建
- 无论采用哪种创建方式，以下 3 个名称必须完全一致：
  - worktree 名称
  - worktree 目录名
  - worktree 分支名

建议命令格式如下：

```bash
git worktree add ../<子任务名称> -b <子任务名称> <基线分支>
```

`基线分支` 默认取当前主工作区所在分支；如果需求计划文件内明确指定，则以计划文件为准。

### 2. `mpc-master-progress`

#### 手动调用示例

```text
/mpc-master-progress
/mpc-master-progress phone_1_requirement_1
```

#### 输入模式

- 无参数：查看全部子任务状态
- 单个参数 `<子任务名称>`：查看单个子任务状态

#### 职责边界

`mpc-master-progress` 必须始终是只读模式，不允许改写 `~/.mpc/{工程项目名}/mpc.md` 或 `~/.mpc/{工程项目名}/mpc_archive.md`。

单任务查询时，应先查 `~/.mpc/{工程项目名}/mpc.md`，未命中再查 `~/.mpc/{工程项目名}/mpc_archive.md`；如果两个文件同时命中同名任务，必须报数据异常。

查看输出规则如下：

- `未开始`：显示 `开始提示词`
- `进行中`：显示已进行多久、当前进度概况、阻塞事项、预计还需要多久、下一步提示词
- `完成`：显示完成时间、累计耗时、完成摘要、完成提示词
- `已回归`：显示开始时间、完成时间、回归时间、累计耗时、回归摘要、归档状态
- 单任务查询时，无论当前状态如何，都应优先显示 `子任务目标`
- 无论是无参数总览还是单任务查询，命令结束时都必须按“任务树输出规范”输出一次最新任务树
- 无参数模式下，任务树应作为主视图；如需补充平铺状态总览，保持简洁即可
- 单任务查询时，先展示目标任务详情，再输出最新任务树

### 3. `mpc-master-regress`

#### 手动调用示例

```text
/mpc-master-regress phone_1_requirement_1
```

#### 输入

- 必填：一个状态应为 `完成` 的 `<子任务名称>`

#### 职责边界

`mpc-master-regress` 只负责对子任务执行回归动作，并在回归通过后把状态从 `完成` 推进到 `已回归`。

该动作只允许在目标子任务状态为 `完成` 时执行。

执行规则如下：

1. 重新读取最新 `~/.mpc/{工程项目名}/mpc.md`
2. 校验目标任务状态必须仍然是 `完成`
3. 按 `完成提示词` 执行主控回归动作，并结合代码改动、自检结果、剩余风险与任务业务推荐标准形成回归证据
   - 证据必须围绕 `子任务目标` 是否真正达成展开
4. 先给出“建议回归通过”或“建议回归不通过”的结论；最终是否通过回归，由当前调用者决定
5. 只有在当前调用者确认回归通过后，才允许把状态更新为 `已回归`
6. 状态改为 `已回归` 时必须同步填写：
   - `回归时间`
   - `回归摘要`
   - `最后更新时间`
7. 如果回归动作失败，或当前调用者认为证据不足：
   - 状态保持 `完成`
   - 不得提前改成 `已回归`
   - 在输出中明确失败原因
8. 无论本轮是否真正推进到 `已回归`，只要没有遇到数据异常，命令结束时都必须按“任务树输出规范”输出一次最新任务树；如果已经写回，则输出写回后的树，否则输出未变更的当前树

### 4. `mpc-master-archive`

#### 手动调用示例

```text
/mpc-master-archive phone_1_requirement_1
```

#### 输入

- 必填：一个状态应为 `已回归` 的 `<子任务名称>`

#### 职责边界

`mpc-master-archive` 只负责归档已回归子任务，不负责查看与回归。

该动作只允许在目标子任务状态为 `已回归` 时执行。

执行规则如下：

1. 重新读取最新 `~/.mpc/{工程项目名}/mpc.md`
2. 校验目标任务状态必须仍然是 `已回归`
3. 将任务块移动到 `~/.mpc/{工程项目名}/mpc_archive.md`
4. 写入：
   - `归档状态 = 已归档`
   - `归档时间 = 当前时间`
5. 从 `~/.mpc/{工程项目名}/mpc.md` 删除该任务块
6. 更新两个文件的 `元信息`
7. 即使当前任务已归档，只要其他未归档子任务仍引用它作为前序任务，也允许归档；后续依赖检查必须能够从 `~/.mpc/{工程项目名}/mpc_archive.md` 命中它
8. 只要没有遇到数据异常，命令结束时都必须按“任务树输出规范”输出一次最新任务树；归档后的任务仍要保留在树中，并显示为 `[已归档]`

### 5. `mpc-slave`

#### 手动调用示例

```text
/mpc-slave
```

#### 任务识别规则

`mpc-slave` 不接收子任务名称参数，而是从当前 worktree 自动识别：

1. 当前 worktree 分支名
2. 当前 worktree 目录名

两者必须完全一致，且必须能在 `~/.mpc/{工程项目名}/mpc.md` 中找到同名 `任务标识`。

如果目录名与分支名不一致，或控制器中不存在该任务，必须立即停止并报错，不允许猜测。

#### 状态驱动行为

##### 当状态为 `未开始`

1. 先检查全部 `前序子任务`
2. 只有当前序子任务全部为 `已回归` 时，才允许启动
3. 启动时原子更新以下字段：
   - `状态 = 进行中`
   - `开始时间 = 当前时间`
   - `最后更新时间 = 当前时间`
   - `预计剩余时间 = 初始估算值`
   - `下一步提示词 = 开始提示词`
4. 然后再按 `开始提示词` 执行子任务

其中补充要求如下：

- 前序任务查询范围必须同时覆盖 `~/.mpc/{工程项目名}/mpc.md` 与 `~/.mpc/{工程项目名}/mpc_archive.md`
- 如果任一前序任务在两个文件中都找不到，必须停止并报错
- `初始估算值` 由任务业务复杂度与当前调用者预估共同给出，格式必须符合 `Xd Xh Xm`

##### 当状态为 `进行中`

`mpc-slave` 每次运行都需要更新进度。

进度信息来源优先级如下：

1. 当前 worktree 下 `./planning/` 中与本子任务相关的最新内容
2. 当前 worktree 的实际代码改动与当前会话上下文
3. 如果 `./planning/` 不存在，或存在但没有可匹配本子任务的内容，则使用第 2 条作为降级方案

其中：

- `./planning/` 中与本子任务相关的内容，优先指文件名包含 `任务标识` 的文件
- 如果存在多个匹配文件，则按修改时间从新到旧取最近一份；若修改时间相同，则按文件名字典序取最后一份作为“最新内容”

更新规则如下：

- 更新 `当前进度概况`，最多 5 行
- 更新 `阻塞事项`
- 更新 `预计剩余时间`
- 更新 `下一步提示词`
- 更新 `最后更新时间`
- 如果 `当前进度概况`、`阻塞事项`、`下一步提示词` 均无实质变化，允许仅更新时间

其中，`子任务目标` 是判断当前任务是否可以进入“建议已完成”的主标准。

如果本轮基于 `子任务目标`、当前产出与自检结果形成“建议已完成”结论，还必须先形成完成证据，并先输出一棵完整的完成决策任务树。

完成决策任务树规则如下：

- 基础结构仍使用“任务树输出规范”的完整树
- 仅当前任务的直接 `后序子任务` 允许追加后续决策标记，不扩散到其他层级
- 直接后序任务追加 `[决策]` 的条件是：该任务当前为 `未开始`，且除当前任务外，其余 `前序子任务` 都已在活动区或归档区满足 `状态 = 已回归`；已归档任务同样视为满足
- `[推荐]` 只允许追加给一个直接候选，默认取 `后序子任务` 列表中第一个满足 `[决策]` 条件的任务
- 节点展示格式固定为 `task_name [未开始] [决策][推荐]` 或 `task_name [未开始] [决策]`
- 如果没有任何满足条件的直接候选，仍输出完整任务树，但不追加 `[推荐]`

只有当前调用者认可“建议已完成”结论时，才允许原子更新：

- `状态 = 完成`
- `完成时间 = 当前时间`
- `累计耗时`
- `完成摘要`
- `最后更新时间`

如果当前调用者认为证据不足或仍有未闭合事项，则状态保持 `进行中`，并继续更新进度信息。

`完成摘要` 至少应覆盖：

- 已完成内容
- 尚存风险
- 自检结果
- 建议主控回归关注点

##### 当状态为 `完成`

- 显示 `完成时间`、`累计耗时`、`完成摘要`、`完成提示词`
- 不允许 `mpc-slave` 自行把任务改成 `已回归`

##### 当状态为 `已回归`

- 显示 `已回归`、`回归时间`、`回归摘要`、`归档状态`
- 提示该任务只能由 `mpc-master-archive <子任务名称>` 归档

## 最小闭环要求

只要 5 个 skills 的实现满足以下闭环，本方案就算成立：

1. `mpc-master-start` 能稳定创建结构正确的子任务
2. `mpc-master-progress` 能稳定提供只读状态总览与任务树
3. `mpc-slave` 能根据当前 worktree 精确命中自己的任务，并在当前调用者确认后推进到 `完成`
4. `mpc-master-regress` 能基于回归证据给出结论，并在当前调用者确认后把 `完成` 的任务安全推进到 `已回归`，同时刷新任务树
5. `mpc-master-archive` 能把 `已回归` 的任务安全移动到归档文件，并在归档后刷新任务树
6. 多个 worktree 同时更新时，不会因为覆盖写导致其他任务信息丢失

## 明确不允许的行为

- 不允许把“查看状态”设计成“顺便改状态”
- 不允许在 `未开始` 阶段绕过前序依赖直接开工
- 不允许 `mpc-slave` 直接把状态从 `进行中` 改成 `已回归`
- 不允许 `mpc-master-progress` 承担回归或归档动作
- 不允许归档 `未回归` 的子任务
- 不允许在未持有 `~/.mpc/{工程项目名}/.lock.md` 时执行任何写入动作
- 不允许只查询 `~/.mpc/{工程项目名}/mpc.md` 就认定前序任务不存在或不可用
- 除 `mpc-master-start` 按规则回填直接相关依赖字段外，不允许在写入一个子任务时重排、重写其他子任务内容
- 不允许省略固定字段，导致后续 skill 无法稳定解析
