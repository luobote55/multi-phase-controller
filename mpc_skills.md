# GSD Phase 控制器（multi-phase-controller，简称 `mpc`）

## 目标

本文档的唯一目的，是为以下 5 个 skills 提供可执行、可并发协作、可持续维护的控制规则：

- `mpc-master-start`
- `mpc-master-progress`
- `mpc-master-regress`
- `mpc-master-archive`
- `mpc-slave`

本文档不是功能宣传文档，而是 5 个 skills 的行为规范。后续实现时，应直接按本规范读写 `<repo-root>/.mpc/mpc.md` 与 `<repo-root>/.mpc/mpc_archive.md`。

## 文件规划

- 共享控制目录：`<repo-root>/.mpc/`
- 实时控制文件：`<repo-root>/.mpc/mpc.md`
- 归档文件：`<repo-root>/.mpc/mpc_archive.md`
- 写入锁文件：`<repo-root>/.mpc/.lock.md`
- `repo-root` 固定指向当前项目的 Git 仓库根目录，而不是调用时所在的子目录
- 同一 Git 仓库的所有会话共享同一个 `<repo-root>/.mpc/` 控制目录
- 不要随着触发位置漂移到其他子目录；始终使用仓库根目录下的 `.mpc/`
- `.lock.md` 为临时锁文件，仅用于串行化写入；只读查询类 skill 不创建该文件
- 共享目录或文件不存在时，由 `mpc-master-start` 或首次归档动作创建基础骨架

## 总体原则

- 主控制器负责拆分、汇总、回归、归档，不负责代替子任务做日常开发推进
- 主控制器侧共包含 4 个 skills：`mpc-master-start`、`mpc-master-progress`、`mpc-master-regress`、`mpc-master-archive`
- 子任务通过显式 `任务标识` 驱动，不再依赖目录或分支自动识别
- 单目录模式是主流程：多个子任务在同一仓库目录中，通过多开对话并行推进
- 默认约束是“同一时刻一个任务只由一个对话推进”；本规范不额外提供会话认领机制
- 子任务默认要求可独立并行；如果存在依赖，必须显式声明 `前序子任务` 与 `后序子任务`
- 只要涉及同文件、同核心模块、路由或注册表、依赖清单、生成产物、共享导出面，或存在明显顺序关系，就必须拆成串行子任务，不能拆成并行子任务
- 如对是否会冲突拿不准，默认按串行任务建模，而不是冒险并行
- `mpc.md` 必须使用固定字段顺序，字段不可省略；没有值时必须写 `待填写` 或 `无`
- 任何查询动作默认只读；任何状态推进动作必须显式触发，不能把“查看”设计成“顺手修改”
- 所有时间统一使用 `YYYY-MM-DD HH:mm:ss +08:00` 格式
- 除 `mpc-master-progress` 外，所有会写入 `<repo-root>/.mpc/mpc.md` 或 `<repo-root>/.mpc/mpc_archive.md` 的动作都必须先持有 `<repo-root>/.mpc/.lock.md`
- `进行中 -> 完成` 与 `完成 -> 已回归` 的推荐判断标准来自子任务业务本身；最终是否推进状态，由当前调用者决定

## 子任务状态机

子任务固定只有 4 种状态：

- `未开始`
- `进行中`
- `完成`
- `已回归`

唯一允许的正常流转路径如下：

`未开始 -> 进行中 -> 完成 -> 已回归`

状态定义如下：

- `未开始`：任务已被拆分并写入控制器，但尚未正式执行
- `进行中`：子任务已开始执行，允许持续更新进度、阻塞项、预计剩余时间
- `完成`：子任务自身开发与自检已结束，等待主控制器执行回归动作
- `已回归`：主控制器已完成该子任务的回归确认，允许归档

状态推进责任如下：

- `mpc-master-start`：只能创建 `未开始`
- `mpc-slave`：负责 `未开始 -> 进行中` 与 `进行中 -> 完成`
- `mpc-master-progress`：只负责查看，不负责推进状态
- `mpc-master-regress`：负责 `完成 -> 已回归`
- `mpc-master-archive`：负责归档 `已回归` 子任务
- 归档不是状态，归档动作只允许发生在 `已回归` 之后

## `mpc.md` 固定结构

`mpc.md` 必须采用以下固定结构。字段顺序固定，后续 skill 应按标题精确读取和更新。

````md
# MPC 控制文件

## 元信息
- 版本：1
- 最后更新时间：2026-04-25 12:00:00 +08:00
- 任务总数：1

# 子任务 phone_1_requirement_1
## 任务标识
phone_1_requirement_1
## 状态
未开始
## 来源计划文件
docs/example_plan.md
## 执行模式
single_dir
## 执行目录
.
## 前序子任务
无
## 后序子任务
phone_2_requirement_2
## 子任务目标
```text
完成 phone_1_requirement_1 的上下文装载与边界确认。
输出可直接支撑后续实现的当前边界、现状与主要风险结论。
```
## 开始时间
待填写
## 完成时间
待填写
## 回归时间
待填写
## 归档时间
待填写
## 最后更新时间
2026-04-25 12:00:00 +08:00
## 累计耗时
待填写
## 预计剩余时间
待填写
## 当前进度概况
```text
待填写
```
## 阻塞事项
```text
无
```
## 下一步提示词
```text
待填写
```
## 开始提示词
```text
执行 gsdphase 0。
目标：完成子任务目标要求的上下文装载与边界确认。
请输出：
1. 当前边界
2. 现状
3. 对子任务目标的完成判断依据
```
## 完成提示词
```text
请对 phone_1_requirement_1 的产出是否满足“子任务目标”做回归确认，并给出是否允许归档的结论。
```
## 完成摘要
```text
待填写
```
## 回归摘要
```text
待填写
```
## 归档状态
未归档
````

补充约束如下：

- 固定字段顺序为：
  `任务标识 -> 状态 -> 来源计划文件 -> 执行模式 -> 执行目录 -> 前序子任务 -> 后序子任务 -> 子任务目标 -> 开始时间 -> 完成时间 -> 回归时间 -> 归档时间 -> 最后更新时间 -> 累计耗时 -> 预计剩余时间 -> 当前进度概况 -> 阻塞事项 -> 下一步提示词 -> 开始提示词 -> 完成提示词 -> 完成摘要 -> 回归摘要 -> 归档状态`
- `执行模式` 当前固定只能写 `single_dir`
- `执行目录` 当前固定只能写 `.`
- `当前进度概况` 最多 5 行
- `阻塞事项` 最多 5 行；没有阻塞时写 `无`
- `子任务目标` 使用带 `text` 信息字符串的 fenced code block，限制 1 到 5 行，内容必须面向结果、可直接用于完成判定，不能只写过程动作
- `下一步提示词` 用于下一轮继续推进当前子任务
- `累计耗时` 与 `预计剩余时间` 统一使用 `Xd Xh Xm` 文本格式；不足位可省略，例如 `4h 30m`
- `当前进度概况`、`阻塞事项`、`下一步提示词` 没有实质变化时，允许保持原文不变，仅更新 `最后更新时间`
- `完成摘要` 至少包含：完成内容、涉及文件或模块、自检命令与结果、剩余风险、建议主控回归关注点
- `回归摘要` 至少包含：回归范围、依据证据、回归结果、是否允许归档
- `归档状态` 只能是 `未归档` 或 `已归档`

## `mpc_archive.md` 结构

- `mpc_archive.md` 的单个子任务结构与 `mpc.md` 完全一致
- `任务总数` 表示当前文件内任务块数量；在 `mpc.md` 中表示当前活动任务数，在 `mpc_archive.md` 中表示当前归档任务数
- 归档时采用“移动”而不是“复制”
- 被归档的子任务必须从 `<repo-root>/.mpc/mpc.md` 删除，并追加到 `<repo-root>/.mpc/mpc_archive.md`
- 子任务查询与依赖解析的范围，是 `<repo-root>/.mpc/mpc.md` 与 `<repo-root>/.mpc/mpc_archive.md` 的并集；默认优先查询 `<repo-root>/.mpc/mpc.md`，未命中时再查询 `<repo-root>/.mpc/mpc_archive.md`
- 如果同名 `任务标识` 同时出现在 `<repo-root>/.mpc/mpc.md` 与 `<repo-root>/.mpc/mpc_archive.md`，视为数据异常，必须立即停止并报错
- 归档后必须满足：
  - `状态` 为 `已回归`
  - `归档状态` 为 `已归档`
  - `归档时间` 已填写

## 并发写入规则

由于 `mpc.md` 会被多个对话读写，而 skills 又由使用者手动触发，因此真实高并发概率通常较低；但为了满足“不丢状态”的目标，所有写入动作仍必须串行化。skills 必须遵守以下规则：

1. 除 `mpc-master-progress` 外，任何写入动作开始前都必须先申请 `<repo-root>/.mpc/.lock.md`
2. `.lock.md` 内至少记录：持有者 skill 名称、目标任务名称或任务列表、当前仓库目录或分支、加锁时间
3. 如果发现 `<repo-root>/.mpc/.lock.md` 已存在，必须等待并重试；默认每 2 秒重试一次，最多等待 60 秒
4. 如果 `<repo-root>/.mpc/.lock.md` 持续超过 10 分钟未释放，应判定为陈旧锁；此时必须停止写入，并明确提示调用者人工处理旧锁
5. 成功获取锁后，必须重新读取最新的 `<repo-root>/.mpc/mpc.md`；必要时同时读取 `<repo-root>/.mpc/mpc_archive.md`
6. 除 `mpc-master-start` 外，每次只允许修改一个子任务块，以及文件头部 `元信息`
7. `mpc-master-start` 允许在单次事务内追加多个新任务块，并以最小改动范围回填直接相关前序任务的 `后序子任务` 字段；不得改写无关任务内容
8. 非目标子任务内容、顺序、提示词不得被重写或折叠
9. 写入前必须校验 `任务标识` 与当前目标一致；`mpc-master-start` 还必须校验待新增任务名称均合法且不重名
10. 如果获取锁后发现当前准备写入的内容不是基于最新文件快照生成，必须基于最新内容重新合并；合并失败时，应停止写入并明确提示冲突，而不是覆盖
11. 写入必须采用“临时文件 + 原子替换”方式，禁止原地覆盖半写入文件
12. 每次成功写入后，必须同步更新：
   - 文件头部 `最后更新时间`
   - 文件头部 `任务总数`
   - 当前子任务的 `最后更新时间`
13. 无论本轮写入成功或失败，只要持有过锁，都必须在结束前释放 `<repo-root>/.mpc/.lock.md`

## 任务拆分规则

`mpc-master-start` 在拆分子任务时必须满足以下条件：

- 子任务名称全局唯一，不能与已有子任务重名
- 子任务名称不能与 `mpc`、`mpc_archive`、`lock` 等保留名称冲突
- 子任务名称只允许使用小写字母、数字、下划线
- 子任务默认以 3 到 10 个可执行步骤为宜
- 只有在强原子性要求下，才允许超过 10 个步骤
- 如果两个子任务存在明显代码冲突、强顺序依赖、共享同一核心改动点，或命中以下任一场景：
  - 同文件
  - 同核心模块
  - 路由或注册表
  - 依赖清单
  - 生成产物
  - 共享导出面
  则应拆成串行子任务，而不是并行子任务，并且必须显式写明 `前序子任务` 与 `后序子任务`
- 如果存在弱依赖，也可以拆分，但必须在字段中写明 `前序子任务` 与 `后序子任务`
- 如果是向现有需求链路追加后续任务，允许最小范围修改直接前序任务的 `后序子任务` 字段，以保持依赖图一致
- 不新增“路径归属检查”或“会话认领机制”；冲突控制主要依赖任务切分、前后序和调用纪律

## 任务树输出规范

`mpc-master-start`、`mpc-master-progress`、`mpc-master-regress`、`mpc-master-archive` 在命令结束时，都必须输出一次“最新任务树”。任务树用于稳定展示当前控制器中的任务依赖关系，不能只用平铺列表替代。

- 任务树数据源是 `<repo-root>/.mpc/mpc.md` 与 `<repo-root>/.mpc/mpc_archive.md` 的并集；如果归档文件不存在，可以忽略
- 如果同名 `任务标识` 同时出现在两个文件中，或树构建过程中发现循环依赖、缺失子任务、同一子任务被多个父任务直接声明为后序任务，必须报数据异常并停止，不能静默猜测
- `前序子任务` 与 `后序子任务` 在存在多个任务时，统一使用 `、` 分隔；没有值时写 `无`
- 根节点判定规则：`前序子任务 = 无`
- 子节点判定规则：父任务 `后序子任务` 中列出的直接后序任务
- 根节点输出顺序：优先保持 `mpc.md` 中任务块原始顺序；仅存在于 `mpc_archive.md` 的根节点追加在后，并保持归档文件中的原始顺序
- 子节点输出顺序：严格按父任务 `后序子任务` 中的出现顺序输出
- 树状图字符统一使用 `├─`、`└─`、`│  ` 与 3 个空格缩进，形成类似：

```text
root_task
├─ child_a
│  └─ grandchild_a1
└─ child_b
```

- 树节点最少输出 `任务标识`
- `mpc-master-progress`、`mpc-master-regress`、`mpc-master-archive` 默认应在节点后追加状态标签，如 `task_name [进行中]`
- 已归档任务在树中统一显示为 `task_name [已归档]`
- `mpc-master-start` 在所有新任务初始都为 `未开始` 时，可以只输出任务名；如果当前控制器中已经存在混合状态任务，也允许统一追加状态标签
- `mpc-master-regress` 与 `mpc-master-archive` 的任务树格式必须复用 `mpc-master-start` 的完整树风格，不允许退化为平铺列表或省略层级

## 5 个 Skills 的详细定义

### 1. `mpc-master-start`

#### 手动调用示例

```text
/mpc-master-start docs/xxxxx.md
```

#### 输入

- 必填：一个需求计划文件路径

#### 行为

1. 读取需求计划文件并分析可并行执行或可串行执行的子任务
2. 为每个子任务生成固定结构的任务块，初始状态统一为 `未开始`
3. 自动生成：
   - `任务标识`
   - `前序子任务`
   - `后序子任务`
   - `子任务目标`
   - `开始提示词`
   - `完成提示词`
4. 所有新任务固定写入：
   - `执行模式 = single_dir`
   - `执行目录 = .`
5. 如果 `<repo-root>/.mpc/mpc.md` 已存在：
   - 必须先检查是否有重名任务
   - 默认只允许追加新任务
   - 如果本次是向现有链路追加后续任务，则允许最小范围修改直接前序任务的 `后序子任务` 字段，但不得改写无关任务内容
6. 如果 `<repo-root>/.mpc/mpc.md` 不存在：
   - 先创建 `<repo-root>/.mpc/`
   - 再创建 `mpc.md` 骨架
7. 命令结束时，输出建议执行的首轮子任务命令列表，但不自动执行

#### 输出要求

- 先按“任务树输出规范”输出本次写入后的最新任务树
- 列出本次新增子任务总数
- 只列出首轮可执行子任务
- 只列出首轮可执行子任务对应的建议启动命令
- 建议启动命令格式固定为：

```text
/mpc-slave <子任务名称>
```

- 首轮可执行子任务的判定标准是：`前序子任务 = 无`，或其前序子任务已经在现有控制文件中处于 `已回归`
- 被前序子任务阻塞的新任务必须体现在任务树中，但不需要再额外用平铺列表逐条展示；这些任务仍然必须写入 `<repo-root>/.mpc/mpc.md`
- 必须明确提示：每个首轮可执行子任务应在独立对话框中推进
- 如果本次新增任务中没有任何首轮可执行项，必须明确输出“首轮可执行任务：无”，并简要说明阻塞来源

### 2. `mpc-master-progress`

#### 手动调用示例

```text
/mpc-master-progress
/mpc-master-progress phone_1_requirement_1
```

#### 职责

- 保持只读模式
- 展示全部活动任务概览，或展示单个任务详情
- 单任务查询时，应先查 `<repo-root>/.mpc/mpc.md`，未命中再查 `<repo-root>/.mpc/mpc_archive.md`

#### 按状态展示规则

- 单任务查询时，无论当前状态如何，都优先显示 `子任务目标`
- `未开始`：显示 `开始提示词`
- `进行中`：显示耗时、进度概况、阻塞事项、预计剩余时间、下一步提示词
- `完成`：显示完成时间、累计耗时、完成摘要、完成提示词
- `已回归`：显示开始时间、完成时间、回归时间、累计耗时、回归摘要、归档状态

### 3. `mpc-master-regress`

#### 手动调用示例

```text
/mpc-master-regress phone_1_requirement_1
```

#### 职责

- 只处理一个当前状态为 `完成` 的任务
- 回归证据优先来自：
  - `完成摘要`
  - 自检命令与结果
  - 当前总代码状态
  - `子任务目标`
- 不再假设一定能拿到该任务独立的 Git diff
- 先给出建议，再等待明确确认
- 只有得到明确确认后，才把任务推进到 `已回归`

#### 特别约束

- 如果 `完成摘要` 缺少“涉及文件或模块”或“自检命令与结果”，且调用者也没有提供额外证据，应优先给出“建议回归不通过”或“证据不足”的结论

#### 通过后写入

- `状态 = 已回归`
- `回归时间`
- `回归摘要`
- `最后更新时间`

如果回归不通过，状态保持为 `完成`。

### 4. `mpc-master-archive`

#### 手动调用示例

```text
/mpc-master-archive phone_1_requirement_1
```

#### 职责

- 只处理一个当前状态为 `已回归` 的任务
- 把该任务从 `<repo-root>/.mpc/mpc.md` 移动到 `<repo-root>/.mpc/mpc_archive.md`
- 必要时创建最小归档文件

#### 归档时写入

- `归档状态 = 已归档`
- `归档时间`
- `最后更新时间`

不得归档 `未开始`、`进行中` 或 `完成` 的任务。

### 5. `mpc-slave`

#### 手动调用示例

```text
/mpc-slave phone_1_requirement_1
```

#### 任务识别规则

`mpc-slave` 必须接收一个显式子任务名称参数，而不是从当前工程目录、git 分支名或最近改动中自动猜测：

1. 读取调用参数中的 `任务标识`
2. 在 `<repo-root>/.mpc/mpc.md` 中查找同名 `任务标识`
3. 如果不存在该任务，立即报错并停止

禁止无参数运行，也禁止自动猜测。

#### 状态推进

- 使用 `<repo-root>/.mpc/mpc.md` 作为控制文件
- 只负责 `未开始 -> 进行中 -> 完成`

#### 当状态为 `未开始`

1. 读取全部 `前序子任务`
2. 依赖检查范围必须覆盖 `<repo-root>/.mpc/mpc.md` 与 `<repo-root>/.mpc/mpc_archive.md` 的并集
3. 只有当前序子任务全部为 `已回归` 时，才允许启动
4. 为 `预计剩余时间` 形成一个初始估算值，格式必须是 `Xd Xh Xm`
5. 原子更新：
   - `状态 = 进行中`
   - `开始时间 = 当前时间`
   - `最后更新时间 = 当前时间`
   - `预计剩余时间 = 初始估算值`
   - `下一步提示词 = 开始提示词`
6. 写回后，按 `开始提示词` 继续推进当前任务

#### 当状态为 `进行中`

每次运行都要更新进度。信息来源优先级固定为：

1. 当前仓库目录下 `./planning/` 中与本任务最相关的最新文件
2. 当前仓库目录的实际代码改动与当前会话上下文

更新时至少处理：

- `当前进度概况`
- `阻塞事项`
- `预计剩余时间`
- `下一步提示词`
- `最后更新时间`

如果基于 `子任务目标`、当前产出与自检结果形成“建议已完成”结论，必须先整理完成证据，并先向调用者展示：

- 已完成内容
- 涉及文件或模块
- 自检命令与结果
- 尚存风险
- 建议主控回归关注点
- 一棵完整的完成决策任务树

完成决策任务树规则如下：

- 基础结构仍使用“任务树输出规范”的完整树
- 仅当前任务的直接 `后序子任务` 允许追加后续决策标记，不扩散到其他层级
- 直接后序任务追加 `[决策]` 的条件是：该任务当前为 `未开始`，且除当前任务外，其余 `前序子任务` 都已在活动区或归档区满足 `状态 = 已回归`
- `[推荐]` 只允许追加给一个直接候选，默认取 `后序子任务` 列表中第一个满足 `[决策]` 条件的任务
- 如果没有任何满足条件的直接候选，仍输出完整任务树，但不追加 `[推荐]`

只有在调用者明确认可“建议已完成”后，才原子更新：

- `状态 = 完成`
- `完成时间 = 当前时间`
- `累计耗时`
- `完成摘要`
- `最后更新时间 = 当前时间`

#### 当状态为 `完成`

只展示：

- `完成时间`
- `累计耗时`
- `完成摘要`
- `完成提示词`

不要把任务改成 `已回归`。

#### 当状态为 `已回归`

只展示：

- `回归时间`
- `回归摘要`
- `归档状态`

并明确提示该任务只能由 `mpc-master-archive <任务名>` 归档。

## 最小闭环要求

以下能力都成立时，这套设计才算真正可用：

1. `mpc-master-start` 能稳定创建结构正确的子任务。
2. `mpc-master-progress` 能稳定提供只读状态总览与任务树。
3. `mpc-slave` 能根据显式 `任务标识` 准确命中自己的任务，并在当前调用者确认后推进到 `完成`。
4. `mpc-master-regress` 能基于完成摘要、自检结果和当前总代码状态给出回归结论，并在当前调用者确认后把 `完成` 的任务安全推进到 `已回归`。
5. `mpc-master-archive` 能把 `已回归` 的任务安全移动到归档文件，并在归档后刷新任务树。
6. 多个对话同时更新控制器时，不会因为覆盖写导致其他任务信息丢失。

## 明确禁止的行为

- 不允许把“查看状态”设计成“顺便改状态”
- 不允许让 `mpc-slave` 绕过前序依赖
- 不允许让 `mpc-slave` 从 `进行中` 直接跳到 `已回归`
- 不允许让 `mpc-master-progress` 承担回归或归档动作
- 不允许归档尚未完成回归的任务
- 不允许在未持有 `<repo-root>/.mpc/.lock.md` 时执行任何写入动作
- 不允许只查询 `<repo-root>/.mpc/mpc.md` 就认定前序任务不存在或不可用
- 不允许在更新一个任务时重写无关任务块
- 不允许省略固定字段，导致下游解析失败
- 不允许让 `mpc-slave` 无参数运行或自动猜测任务名
